import React from 'react';

const Header = () => {
  return (
    <header className="main-header">
      <h2 className="main-header-title">Curso completo de JavaScript</h2>
    </header>
  );
};

export default Header;
